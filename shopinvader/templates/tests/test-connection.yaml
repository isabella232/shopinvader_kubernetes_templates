apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopinvader.fullname" . }}-test-connection"
  labels:
    {{- include "shopinvader.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopinvader.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
